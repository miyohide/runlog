<%= form_with(model: course, local: true, class: 'uk-form-horizontal') do |form| %>
  <% if course.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(course.errors.count, "error") %> prohibited this course from being saved:</h2>

      <ul>
        <% course.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field uk-margin">
    <%= form.label :name, class: 'uk-form-label' %>
    <div class='uk-form-controls'>
      <%= form.text_field :name, class: 'uk-input' %>
    </div>
  </div>

  <div class="actions uk-margin">
    <%= form.submit class: 'uk-button uk-button-primary' %>
  </div>
<% end %>
